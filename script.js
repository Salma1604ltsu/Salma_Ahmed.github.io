const terminalLines=[['whoami','cybersecurity_analyst'],['focus','VAPT / SOC / Threat Detection'],['tools','Burp Suite / Nmap / Metasploit / Splunk'],['status','available for security opportunities']];let i=0;const out=document.querySelector('#live-terminal');function run(){if(!out)return;const [cmd,result]=terminalLines[i%terminalLines.length];out.innerHTML=`<span>$ ${cmd}</span><br><b>${result}</b>`;i++;setTimeout(run,2600)}run();document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const el=document.querySelector(a.getAttribute('href'));if(el){e.preventDefault();el.scrollIntoView({behavior:'smooth'})}}));